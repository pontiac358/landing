{/*  */}

Un indicador que muestra la finalización de una tarea: carga, descarga de datos, procesamiento, etc., hasta que la interfaz esté completamente lista para la interacción del usuario. El indicador no muestra un progreso medible, sino que solo indica que el proceso aún no ha terminado y continúa.

### **Uso**

✅ **Cuándo usar**

1. Cuando la carga tarda más de 300 ms de espera. `Loader` no debe aparecer durante períodos de tiempo muy cortos, ya que esto provoca un parpadeo molesto. Si ha mostrado `Loader`, evite el efecto de parpadeo. Si la carga tarda menos de 300 ms, no muestre nada.
2. Cuando no se puede predecir la apariencia del contenido después de la carga.
3. Si `Loader` se utiliza para carga asíncrona, debe mostrarse exactamente en el lugar donde ocurre la carga. La carga de un solo elemento no debe bloquear el resto de la interfaz.

{/*  */}


🚫 **Cuándo no usar**

1. Si la carga tarda más de 10 segundos y sabemos cuánto tiempo llevará, es mejor mostrar una barra de progreso.
2. Si se puede predecir la apariencia del contenido, para tales casos, un esqueleto podría ser adecuado y tiene sentido usarlo.
3. Si la carga se produce al hacer clic en un botón, el estado del propio botón será suficiente para la retroalimentación, por ejemplo, en ventanas modales.

### Tamaños

![Sizes](/static/images/design/Loader/Sizes.png)

1. **S** — tamaño base. Se utiliza en campos de entrada, ventanas emergentes y celdas de tablas, y en otros casos donde el espacio es limitado.
2. **M** — tamaño mediano, se utiliza en tarjetas y tablas.
3. **L** — tamaño grande, se utiliza en páginas completas y ventanas modales (de diálogo) grandes.

### Ejemplos de uso

1. Cuando el contenido aún no ha aparecido.

   ![Example](/static/images/design/Loader/Example5.png)

2. Sobre el contenido, si se está actualizando o se le están realizando cambios. Ejemplo:

   ![Content transparency 30%](/static/images/design/Loader/Example1.png)

   Transparencia del contenido 30%

   {/* ![Loader on a background layer](/static/images/design/Loader/Example2.png)

   `Loader` en una capa de fondo */}

Aquí tienes la traducción del archivo fuente de la guía de diseño al español, manteniendo un tono natural y conservando los componentes MDX, bloques de código y términos técnicos:

3. También puedes intentar evitar que el `Loader` se superponga al contenido si hay espacio para ello.

   ![Ejemplo](/static/images/design/Loader/Example3.png)

4. Puedes añadir una pista, pero solo si aporta información útil, por ejemplo, para explicar lo que está sucediendo en este momento. No es necesario escribir que la carga está en curso, ya que el usuario lo entenderá por el propio elemento `Loader`.

   ![Ejemplo](/static/images/design/Loader/Example4.png)