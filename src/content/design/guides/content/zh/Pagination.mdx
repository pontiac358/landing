组件允许用户按页浏览内容，例如在表格中。

![IMG - 8.png](/static/images/design/Pagination/paginator-0001.png)

## Применение (Usage)

✅ **Когда применять (When to use)**

- Для всех случаев с постраничным выводом. (For all cases with paginated output.)
- Если может потребоваться «перепрыгнуть» через несколько страниц или сразу перейти к нужной. (If it might be necessary to "jump" over several pages or go directly to the desired one.)
- В тех случаях, когда будет полезно настраивать отображение числа строк. (In cases where it would be useful to adjust the display of the number of rows.)
- Для фиксирования размера блоков внутри страницы, чтобы автоматическая подгрузка контента верхнего постоянно не сдвигала нижний. (To fix the size of blocks within a page, so that automatic loading of content at the top does not constantly shift the bottom.)

![3.png](/static/images/design/Pagination/paginator-0002.png)

🚫 **Когда не следует применять (When not to use)**

В случаях, когда важно обеспечить бесшовный переход между страницами выдачи, рекомендуем использовать автоматическую подгрузку или подгрузку по кнопке, если это не затормозит работу страницы. (In cases where it is important to ensure a seamless transition between pages of results, we recommend using automatic loading or loading by button, if it does not slow down the page's performance.)

<div className="row">
   <div className="col-12 col-md-6">
      ![2.png](/static/images/design/Pagination/paginator-0003.png)
   </div>
   <div className="col-12 col-md-6">
      ![1.png](/static/images/design/Pagination/paginator-0004.png)
   </div>
</div>

## Структура (Structure)

![IMG - 18.png](/static/images/design/Pagination/paginator-0005.png)

1. **Текущая страница** — показывает порядковый номер отображаемой страницы. (Current page — shows the sequential number of the displayed page.)
2. **Предыдущая страница** — открывает предыдущую страницу. (Previous page — opens the previous page.)
3. **Следующая страница** — открывает следующую страницу. (Next page — opens the next page.)
4. **Последняя страница контента** — показывается, если известно общее количество контента и его можно сразу поделить на страницы. По клику открывает последнюю страницу. (Last page of content — is shown if the total amount of content is known and can be immediately divided into pages. Opens the last page on click.)
5. **Иконка переполнения** — появляется, если страниц больше, чем 4. (Overflow icon — appears if there are more than 4 pages.)
6. **Переключатели вперед/назад** — переключают страницы по порядку. (Forward/backward toggles — switch pages sequentially.)
7. **Переход к первой странице** — открывает первую страницу контента. (Go to first page — opens the first page of content.)
8. **Переход к конкретной странице** — при вводе номера страницы и нажатии на Enter открывает указанную страницу. (Go to specific page — upon entering the page number and pressing Enter, opens the specified page.)
9. **Количество элементов на одной странице** — переключатель количества отображаемых элементов на одной странице. (Number of items per page — a toggle for the number of items displayed on one page.)

Использовать все элементы структуры вместе не обязательно. Доступные конфигурации перечислены в разделе «Модификации». (It is not necessary to use all structural elements together. Available configurations are listed in the "Modifications" section.)

## Размеры (Sizes)

По умолчанию размер кнопок, инпута и селекта в пагинаторе — `M`. (By default, the size of buttons, input, and select in the paginator is `M`.)

![Frame 2087326211.png](/static/images/design/Pagination/paginator-0006.png)

## 状态

![IMG - 19.png](/static/images/design/Pagination/paginator-0007.png)

**第一页** — 显示第一页。

**第二页/下一页** **页** — 显示第二页及后续页面的状态。

**最后一页** — 显示最后一页。

## 变体

![IMG - 20.png](/static/images/design/Pagination/paginator-0008.png)

1. **页面大小选项和显示输入框** — 当无法跳转到特定页面或控制每页显示的内容数量时，可以禁用这些字段。
2. **总数未知** — 只显示当前页面的变体。当未知元素总数时使用。
3. **隐藏页码** — 没有页码的页面切换器。当列表元素是动态的，并且页面数量和页码会不断变化而让用户感到困惑时使用。
4. **紧凑型** — 没有文本的紧凑型变体。当界面区域受限时使用（例如，对话框）。

对于移动设备界面，可以使用移动版分页器。

![IMG - 23.png](/static/images/design/Pagination/paginator-0009.png)